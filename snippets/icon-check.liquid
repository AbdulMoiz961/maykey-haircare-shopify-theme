<svg class="{{ class | default: "icon" }}" width="{{ size | default: 12 }}" height="{{ size | default: 12 }}" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 6l3 3 5-5"/></svg>
